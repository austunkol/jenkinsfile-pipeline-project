pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo "No pain No Gain"
                sh 'echo using shell within Jenkinsfile'
                echo 'not using shell in the Jenkinsfile'
            }
        }
    }
}