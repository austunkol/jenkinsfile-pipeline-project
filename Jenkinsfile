pipeline {
    agent any
    stages {
        stage('run') {
            steps {
                echo 'Congrastww!!!!'
                sh 'python --version'
                sh 'python pipeline.py'
            }
        }
    }
}